function add(a, b) {
    let addSum = a + b;
    return addSum;
}

function subtract(a, b) {
    let subSum = a - b;
    return subSum;
}

function multiply(a, b) {
    let multiSum = a * b;
    return multiSum;
}